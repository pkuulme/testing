<tr>
	<td align="left"><%= link_to post.title, authors_post_path(post) %></td>
	<td><%= link_to 'Edit', edit_authors_post_path(post), class: 'btn btn-sm btn-warning' %></td>
	<td><%= link_to 'Remove', authors_post_path(post), method: :delete, data: { confirm: 'Are you sure you want to delete this post?'},class: 'btn btn-sm btn-danger' %></td>
</tr>